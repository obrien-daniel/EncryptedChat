<Window x:Class="Client.PopupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Enter Chat Room Name" Height="169.307" Width="330.297">
    <StackPanel Margin="5,5,5,5">
        <TextBlock Name="TitleTextBox" Margin="0,0,0,10" />
        <TextBox Name="InputTextBox" Padding="3,3,3,3" />
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button Name="buttnOk" Content="OK" Grid.Column="0" Margin="0,0,5,0" Padding="8" Click="buttnOk_Click" />
            <Button Name="buttonCancel" Content="Cancel" Grid.Column="1" Margin="5,0,0,0" Padding="8" Click="buttonCancel_Click" />
        </Grid>
    </StackPanel>
</Window>